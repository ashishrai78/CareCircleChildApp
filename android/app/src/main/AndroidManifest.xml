<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============ NETWORK ============ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- ============ LOCATION ============ -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ============ FOREGROUND SERVICE (Android 14+ requires typed perms) ============ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- ============ NOTIFICATIONS (Android 13+) ============ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ============ BOOT & AUTO-START ============ -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.QUICKBOOT_POWERON" />
    <uses-permission android:name="android.permission.RESTART_PACKAGES" />

    <!-- ============ WAKE & ALARMS ============ -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- ============ BATTERY OPTIMIZATION BYPASS ============ -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- ============ USAGE STATS ============ -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- ============ AUDIO ============ -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- ============ QUERY APPS ============ -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- ============ OVERLAY ============ -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- ============ FOREGROUND LAUNCH (Android 14+) ============ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- ============ CONTACTS ============ -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- ============ CALL DETECTION ============ -->
    <!-- 🔥 READ_PHONE_STATE: Detects call state changes (RINGING/OFFHOOK/IDLE) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 🔥 READ_CALL_LOG: Direct call log access (Android 9 and below only) -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <!-- 🔥 PROCESS_OUTGOING_CALLS: Detect outgoing calls -->
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" tools:ignore="ProtectedPermissions" />



    <application
        android:label="CareCircle"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:requestLegacyExternalStorage="false"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 🔥 Notification channel for flutter_background_service plugin -->
        <meta-data
            android:name="id.flutter.flutter_background_service.notification.channel_id"
            android:value="carecircle_service" />
        <meta-data
            android:name="id.flutter.flutter_background_service.notification.channel_name"
            android:value="CareCircle Service" />
        <meta-data
            android:name="id.flutter.flutter_background_service.notification.channel_description"
            android:value="Keeps monitoring running in background" />
        <meta-data
            android:name="id.flutter.flutter_background_service.notification.icon"
            android:value="@mipmap/ic_launcher" />

        <!-- ============ MAIN ACTIVITY ============ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============ FLUTTER BACKGROUND SERVICE ============ -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location|microphone|dataSync"
            android:icon="@mipmap/ic_launcher"
            android:stopWithTask="false"
            tools:replace="android:exported, android:foregroundServiceType" />

        <!-- ============ MASTER FOREGROUND SERVICE (Option B architecture) ============ -->
        <service
            android:name=".CareCircleForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Continuous parental control monitoring for child safety — requires persistent location tracking, real-time data sync with parent app, and protection against unauthorized app tampering" />
        </service>

        <!-- ============ ACCESSIBILITY WATCHDOG ============ -->
        <service
            android:name=".AccessibilityWatchdogService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:stopWithTask="false">

            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ============ CALL DETECTOR SERVICE (real-time call tracking) ============ -->
        <service
            android:name=".CallDetectorService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false" />



        <!-- ============ BOOT RECEIVER ============ -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="false">

            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <!-- ❌ REMOVED: LOCKED_BOOT_COMPLETED (requires directBootAware=true) -->
            </intent-filter>
        </receiver>

        <!-- ============ RESTART RECEIVER ============ -->
        <receiver
            android:name=".RestartReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="com.example.background.RESTART_WATCHDOG" />
            </intent-filter>
        </receiver>

        <!-- ============ NOTIFICATION LISTENER SERVICE ============ -->
        <service
            android:name=".CareCircleNotificationListener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- ============ SECRET CODE RECEIVER ============ -->
        <receiver
            android:name=".SecretCodeReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SECRET_CODE" />
                <data android:scheme="android_secret_code" />
            </intent-filter>
        </receiver>

        <!-- ============ ACCESSIBILITY REVOKED RECEIVER ============ -->
        <receiver
            android:name=".AccessibilityRevokedReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.background.ACCESSIBILITY_REVOKED" />
            </intent-filter>
        </receiver>

        <!-- ============ DEVICE ADMIN RECEIVER (anti-uninstall + remote lock) ============ -->
        <receiver
            android:name=".CareCircleDeviceAdminReceiver"
            android:label="@string/device_admin_label"
            android:description="@string/device_admin_description"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">

            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />

            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLE_REQUESTED" />
                <action android:name="android.app.action.ACTION_PASSWORD_CHANGED" />
                <action android:name="android.app.action.ACTION_PASSWORD_FAILED" />
                <action android:name="android.app.action.ACTION_PASSWORD_SUCCEEDED" />
            </intent-filter>
        </receiver>

    </application>

    <!-- ============ QUERY PACKAGES ============ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

</manifest>